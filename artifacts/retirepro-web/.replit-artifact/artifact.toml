kind = "web"
previewPath = "/"
title = "RetirePro"
version = "1.0.0"
id = "artifacts/retirepro-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25680

[services.development]
run = "pnpm --filter @workspace/retirepro-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/retirepro-web", "run", "build" ]
publicDir = "artifacts/retirepro-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25680"
BASE_PATH = "/"
